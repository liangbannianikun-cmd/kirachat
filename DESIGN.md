# 设计约束

## 信息架构

微信提供的是熟悉的“会话 → 联系对象 → 辅助内容 → 个人设置”心智模型。澄语把它映射为：

| 微信式位置 | 澄语内容 |
| --- | --- |
| 消息 | 显示单聊与群聊时间线；右上角统一新建群聊或添加角色 |
| 联系人 | 双列角色库，集中承担浏览与角色管理 |
| 发现 | 世界书 |
| 我 | 人格、连接、隐私与关于 |

界面不使用微信商标、图标或素材。

四个一级入口使用距屏幕左右 14dp、底部 10dp 的悬浮胶囊 Dock。Dock 与页面内容之间保留安全区，不遮住列表末项；当前入口同时用浅绿色胶囊、绿色线性图标和文字表达，未选中入口保持次级文字色，不能只依赖颜色区分。

角色库点击后先进入角色资料页：顶部使用无标题返回栏和角色头像资料块，中部用白色资料行与灰色分隔带组织角色描述、开场白、世界书和创作者备注，底部以居中的“发消息”“语音通话”承接下一步。聊天内点击标题也可进入同一资料页。

角色头像本身是可操作对象，右下角显示轻量编辑标记。选择图片后先验证格式和 15 MB 上限，再复制到应用私有目录；角色卡只保存私有文件路径，避免相册授权失效后头像丢失。

群聊创建器直接列出全部本地角色，群成员保存稳定的角色卡 ID，不依赖外部服务器。群消息延续微信式左右布局：本人固定在右侧，角色在左侧并额外显示发言名；群组与消息保存在应用私有存储。

“我的”页个人头像与角色头像使用同一套交互：点击选择图片，验证格式和 15 MB 上限后复制到应用私有目录。单聊与群聊右侧头像都读取同一用户头像路径。

## MIUIX 视觉语言

- 页面背景：`#F4F4F6`
- 主表面：`#FFFFFF`
- 主文字：`#18181A`
- 次文字：`#76767C`
- 操作绿：`#07C160`
- 用户气泡：`#95EC69`
- 本人消息：整行右侧锚定，气泡在前、本人人格头像在最右侧；角色回复镜像排列
- 页面卡片圆角：18dp
- 输入框与按钮圆角：11–14dp
- 页面左右安全间距：14–18dp
- 大标题：31sp，轻微负字距
- 正文：14–16sp，使用系统字体并跟随系统字号
- 深色模式跟随系统：背景 `#101114`、主表面 `#1C1D21`、次表面 `#26272C`、主文字 `#F2F2F5`；聊天底色和用户绿色气泡分别降低明度，避免整页纯黑或高亮荧光

MIUIX Compose 当前主线依赖 Kotlin 2.4 / Compose Multiplatform 1.11，和本项目的 Android 8–10 构建基线不匹配，因此这里使用原生 Android View 实现等价组件语言；没有引入只在新系统工作的模糊模块。

## Apple 流体交互原则

- 触摸按下立即把可操作对象缩放到 `0.97`（列表为 `0.985`）。
- 松开或取消时，复用同一组 `SpringAnimation` 并重定向到 `1.0`，不等待旧动画结束。
- 弹簧阻尼比为 `1.0`，无无缘由的回弹；刚度为 `900`，保持短促。
- 发送、连接成功等明确提交点才触发轻触觉反馈。
- 系统关闭动画或启用触摸探索时，缩放直接切换，保留反馈但移除运动。
- 流式输出期间不锁住返回、滚动或停止操作。
- 消息列表或角色资料进入聊天时，聊天页从右侧进入，原页面轻微向左后退；返回沿同一路径反向退出，建立明确的空间关系。系统关闭动画时只保留 100–120ms 淡入淡出。
- Dock 切换页面使用 18dp 方向提示和无回弹弹簧，快速再次切换会直接从当前显示状态重定向。
- 切到后台不会主动中止生成，避免移动端失焦导致推理被取消。
- GPT 登录拆成“获取验证码 → 用户主动打开浏览器 → 等待授权”三个可中断状态；Claude 凭据保存仍是即时、可撤销的本机操作。返回键始终可用，取消会立即断开轮询。
- 登录成功、连接成功等状态就地显示并配合文字说明，不用阻塞式进度对话框遮住页面。

## 连接层级

- 直连 API 是唯一可编辑的服务器连接，可选择 GPT 兼容、Responses、Claude Messages、Gemini GenerateContent、Azure OpenAI 或 Ollama 协议。
- “账户”属于生成身份而不是第二个 API 地址：GPT 使用 OpenAI 设备授权；Claude 使用 Keystore 加密保存的 API Key / Bearer，不提供账号密码输入框。
- 群聊、角色卡与世界书均为本地数据，不随生成路由变化。
- 模型字段是可编辑的自动完成控件：页面打开、协议切换、直连测试成功或账户配置成功时自动刷新，失败时保留按协议/账户隔离的上次成功缓存，不阻塞手动输入。
- Realtime 语音是独立能力：手机向用户控制的令牌服务获取短期 client secret，再连接 `gpt-realtime-2.1`；长期 OpenAI API Key 只存在服务端。
- 语音页负责麦克风权限、24 kHz PCM 采集/播放、语义 VAD、插话清空、静音、扬声器与挂断。页面销毁时释放音频设备和网络连接。
- 当前 Android 8–10 原生基线先使用短期令牌 WebSocket 验证端到端能力；面向生产和复杂网络环境时，传输层应升级为 WebRTC。

## 无障碍

- 所有图标按钮都有中文 `contentDescription`。
- 文字使用 `sp`，容器不强制固定正文高度。
- 颜色之外仍以文字表达连接模式与失败状态。
- 最小主要点击高度为 42–48dp。
- “移除动画”设置受尊重。
